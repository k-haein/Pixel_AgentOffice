# 사용자 작업 관습 + 트리거 명령어 아카이브

> 사용자의 반복되는 말투·결정 패턴·트리거 명령어를 모은 메타 문서.
> Claude는 새 세션 시작 시 이 파일을 보고 사용자가 어떤 명령을 어떻게 쓰는지 학습한다.
> 새 패턴 발견 시 이 파일에 누적해 갱신한다.

---

## 1. Day(일자) 구분 규칙

브레인스토밍 로그(`ideas/00-brainstorming-log.md`)는 `Day 1`, `Day 2`, ... 단위로 섹션이 나뉜다. **Day의 시작과 끝은 사용자가 명시적으로 선언**한다. Claude가 임의로 Day를 끊거나 새 Day를 만들지 않는다.

### Day 시작
- 사용자 발화 예: `"지금부터 day8이야"`, `"오늘은 ~ 만들려고 해"`
- 명시 없으면 *직전 Day의 연속*으로 간주. 새 작업 묶음에 헤더가 필요하면 `Day N (계속)` 같은 sub-section을 쓴다.

### Day 종료 — 3단 합의 필수
1. 사용자: `"오늘은 여기까지 하자"` 같은 마무리 문구
2. Claude: `"Day N 마무리할까요?"` 확인 질문
3. 사용자: `"응"` / `"어"` / `"좋아"` 같은 동의

**3단이 모두 성립해야 Day가 닫힌다.** 사용자가 마무리 문구를 말하지 않으면 Claude가 먼저 끝낼 수 없다.

### 위반 사례 (기억용)
- 2026-05-18 (Day 8 연속) — Claude가 임의로 §76부터 `Day 9` 헤더로 시작 → 사용자 지적, `Day 8 (계속)`으로 정정.
- 2026-05-19 (Day 9) — "세션 정리해" 시 brainstorming-log만 갱신 + HANDOFF §3 미커밋 표 stale 방치 → 태블릿 다음 세션 헷갈림 → 사용자 지적, 트리거에 HANDOFF 동기화 강제 박음.
- 2026-05-20 (Day 10) — "세션 정리해" 시 brainstorming + HANDOFF 갱신했지만 **`FEATURES.md` 누락** (말풍선 5 emotion / forcedNight overlay / 눈 감기 / input 복구 / layout 재구성 명세 안 박음) → 사용자 지적, 트리거에 `FEATURES.md` 갱신 강제 박음.

---

## 2. 트리거 명령어 매핑

| 트리거 (사용자 발화) | Claude 행동 | 비고 |
|---|---|---|
| `"세션 저장해"` / `"세션 정리해"` / `"여기까지 세션 저장해"` | **세션 정리 체크리스트 (정형 3 + 조건부 3)** — 자세히는 §7 | 누락 시 다음 세션 혼란. 정형 3개는 무조건 |
| `"커밋해"` / `"커밋하자"` | **사용자 사전 승인 후** 의미 단위 분할 커밋. 한글 메시지, "무엇/왜/어떻게" 구조. 태블릿(원격) 환경이면 `📱` prefix. | Stop hook 알림은 승인 대체 아님. |
| `"푸시해"` / `"푸시하자"` | **사용자 사전 승인 후** 지정 브랜치로 `git push -u origin <branch>`. 실패 시 2/4/8/16s 지수 백오프 재시도. | |
| `"분석해줘"` / `"분석해서 먼저 알려줘"` | 코드 작성 *전*에 현황 점검 + 옵션 N개 제시 + 추천. 사용자 선택까지 코드 수정 X. | |
| `"이게 뭐야?"` / `"X가 뭔데?"` | 비개발자도 이해 가능한 수준으로 개념 설명. 필요 시 md로 저장 후 포트폴리오에도 반영. | "Electron이 뭔데?" → `ideas/13-electron-and-mobile-strategy.md` 신규 사례. |
| `"~하자"` / `"~로 가자"` | 옵션 선택 또는 진행 결정 시그널. | 예: `"A로 하자"` → 옵션 A 선택. |
| 숫자/문자 단답 (`"1"`, `"응"`, `"어"`, `"좋아"`) | 직전 Claude 질문에 대한 답변. | 옵션 번호 또는 동의. |

---

## 3. 사용자 말투 특징

분석 대상: `ideas/00-brainstorming-log.md` 전체 + 최근 세션.

### 어조
- **한국어 기본 + 영어는 기술 용어만**: Electron, Phaser, Playwright, Anthropic 등은 영어 그대로.
- **짧고 단도직입적**: 한 문장으로 결정·요청 완료. 군더더기 없음.
  - 예: `"커밋해"`, `"푸시해"`, `"1"`, `"A로 하자"`, `"응"`
- **평어체 (~해/~해줘)**: 친밀하고 직접적. 존댓말·격식체 거의 없음.
- **직설적 지적**: 잘못된 행동·결과는 단도직입.
  - 예: `"너가 알아서 커밋푸시한거야?"` / `"너 마음대로 Day를 수정해서는 안돼"` / `"flash 해도 안된다니까 다시 확인해봐"`

### 의문 패턴
- `"~할수있어?"` — 능력/가능성 확인. 보통 진행 신호 *전* 단계.
  - 예: `"이 프로젝트를 내가 수정하고 커밋할수있어?"` → 답한 뒤 본격 지시.
- `"~ 못 ~ 나?"` — 가능성 의심.
  - 예: `"마일스톤 스냅샷은 태블릿에서 못 찍나?"` → Claude가 가능 여부 + 권장사항 답변.
- `"~ 어떻게 ~?"` — 방법론 묻기.

### 결정 표현
- 옵션 중 **번호 또는 알파벳 단독**: `"1"`, `"A로 하자"`
- **"~로 가자" / "~하자"**: 진행 결정. 그 시점에 Claude는 그 옵션으로 즉시 작업.
- 진행 거부 / 지연: `"고민이 드네..."`, `"일단 ~"`

---

## 4. 의사결정 패턴

### 1) Claude가 옵션 제시 → 사용자가 단답으로 선택
- Claude는 항상 **2~4개 옵션** + 각각의 트레이드오프 + 추천을 제시.
- 사용자는 번호/문자로 빠르게 답.
- 예: B-4 책상 회전 시 A/B/C 3개 옵션 → 사용자 `"A로 하자"`.

### 2) 사용자가 큰 그림 → Claude가 세부 정리
- 사용자: 비전·아이디어·제약 제시 ("모바일 출시 결심", "백엔드 + BYOK")
- Claude: 옵션·트레이드오프·로드맵으로 정리 → 사용자가 그중 선택.

### 3) 작업 *전* 분석 요청 → 코드 작성 동의 대기
- `"분석해서 먼저 알려줘"` 패턴.
- Claude는 분석·계획만 내놓고, 사용자가 명시적 진행 신호 줄 때까지 코드 손대지 않음.

### 4) 의문 제기 = 진행 지시 아님
- `"~할 수 있어?"` 류 의문은 확인 단계. 답을 듣고 *별도로* 진행 지시함.
- Claude는 의문에 답만 하고 멈춤. 추가 행동 금지.

### 5) 위반 시 즉각 교정 요구
- 사용자가 룰 위반 발견하면 즉시 지적 + 새 룰 정립 요구.
- Claude는 사과 + 룰 영구화 (CLAUDE.md / 본 파일) + 향후 적용 보장.

---

## 5. 사용자 환경 / 작업 맥락

- **워크스테이션**: PC(데스크탑) — Phaser 시각 작업·`pnpm dev`·E2E 실행. 풀 코드 작업.
- **태블릿 (원격 Claude Code Web)**: 기획 문서·HANDOFF 갱신·가벼운 코드 텍스트 수정. 시각 검증 *불가*. 커밋은 `📱` 마커 필수.
- **GitHub**: `k-haein/Pixel_AgentOffice` (private). 작업 브랜치 push 후 데스크탑에서 pull.

---

## 6. 추가될 패턴 — 누적 갱신

이 파일은 *살아있는* 문서다. 새 트리거·새 말투·새 룰 발견 시 즉시 추가하거나 사용자에게 추가 여부 묻는다.

### 6-1. 오타 점검 필수 (2026-05-27, Day 13 — 사용자 강한 지적)

**모든 작업(코드·문서·UI 한글 텍스트·커밋 메시지·응답) 전후에 오타를 반드시 확인한다.** 사용자: "오타 좀 잡아 제발... 뭔 작업 하기 전에 오타부터 확인해".

- 반복된 실수: **한자 혼입**("양"→"両"), **깨진 글자**("릌릌하게" — 한글을 `\uXXXX`로 변환 시 발생), 맞춤법
- UI에 노출되는 한글(라벨·팻말·버튼·안내문)은 작성 직후 다시 읽고 검토
- AskUserQuestion 한글은 유니코드 이스케이프 대신 평문으로 작성 (깨짐 방지)
- 자세히는 메모리 `feedback_proofread_before_work.md`

---

## 7. 세션 정리 체크리스트 (트리거 시 자동 진행)

`"세션 저장해" / "세션 정리해"` 시 Claude가 **다음 6 항목을 모두 점검**. 정형 3개는 *무조건*, 조건부 3개는 *해당 시*:

### 정형 (매번 — 무조건)

- [ ] **`ideas/00-brainstorming-log.md`** — 현재 Day 섹션에 신규 번호로 append. 의사결정 흐름·"왜 그렇게"·산출 커밋·인사이트·위반 사례 포함
- [ ] **`HANDOFF.md`** 5개 섹션 동기화:
  - [ ] 최종 갱신일자 헤더
  - [ ] §1 30초 요약 (현재 마일스톤 / 다음 작업 / 큰 결정 / 검증 상태)
  - [ ] §2 진행 타임라인 — Day 섹션 추가
  - [ ] §3 미커밋 표 → "현재 상태 + 보류 fix" 로 정리 (stale 표 금지)
  - [ ] §4 다음 작업 가이드 — 완료 옵션 ✅ 표시 + 새 옵션 추가
- [ ] **`FEATURES.md`** — 이 세션에 *새 기능* 추가됐으면 무조건 명세 박기. 각 기능별 *무엇 / 사용 방법 / 기대 동작 ☐ / 알려진 한계* 형식. 검증 절차 있으면 같이.

### 조건부 (해당 시)

- [ ] **`ideas/06-decisions-to-make.md`** — 새 보류 결정·아이디어 등록됐으면 새 항목 추가 (Day 10 §P 멀티모달 같은)
- [ ] **`ideas/NN-*.md` 신규 회고·진단 md** — 큰 결정·이슈 정리 (예: ideas/14 Platform Adapter / ideas/15 PC 검증 피드백 / ideas/16 캐릭터 v2 / ideas/17 상점 / ideas/18 SSL inspection)
- [ ] **`portfolio/PixelAgentOffice/milestones/M?-?/`** 스냅샷 — *M 마일스톤이 닫힐 때만*. 코드 + retrospective.md

### 마무리

- [ ] 의미 단위 분할 커밋 (사용자 사전 승인 후) — 한글 메시지, 무엇/왜/어떻게 구조, 태블릿이면 `📱` prefix
- [ ] push (사용자 사전 승인 후)
- [ ] 다음 세션 진입 점 명시 (HANDOFF §3 "다음 세션 우선 검증" 등)

### 위반 시 즉시 보고

체크리스트 중 *모르고 누락*하면 사용자에게 즉시 알리고 보완. *임의로 생략 금지*.
