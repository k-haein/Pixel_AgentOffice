# PixelAgentOffice — Product Requirements Document

> **"AI 에이전트를 픽셀 직원처럼 채용·관리하는 게임형 데스크탑 앱"**

| 항목 | 내용 |
|---|---|
| **프로젝트 명** | PixelAgentOffice |
| **유형** | 데스크탑 앱 (Electron) · 개인 학습/포트폴리오 |
| **기간** | 2026-05-12 ~ 진행 중 (Day 12) |
| **버전** | **v0.0.1** — 외부 테스터 배포본 (GitHub Releases portable EXE) |
| **상태** | M5 시그니처 폴리시 완성 + 감정/MBTI/상점·가구/모달 시스템 + **portable .exe 외부 테스터 배포 중** |
| **읽는 시간** | ☕ 10~12분 (skim 3분) |
| **저장소** | private — 코드/기획/시안 일체 보관 |
| **시안** | [wireframes.html](wireframes.html) · [user-flow.md](user-flow.md) · [FEATURES.md](FEATURES.md) |
| **이전 버전** | [v0.0.0 PRD](../v0.0.0/PRD.md) (Day 7, M3 시점 — 초기 스냅샷 보존) |

---

## 📌 30초 요약

AI 에이전트는 강력하지만 **일반 사용자에겐 진입 장벽이 너무 높다**. 코드, 터미널, JSON 설정, 추상적 오케스트레이션 개념. 그런데도 많은 일반 사용자가 ChatGPT/Claude 앱에서 **"인격을 부여한 페르소나 채팅"**(편집자, 칸트, 작가 등)을 일상적으로 사용한다.

**PixelAgentOffice는 이 행동을 "픽셀 사무실 + 직원 관리 게임"으로 게임화**한다:
- 🐙 픽셀 캐릭터를 "직원"으로 채용 (Mary 편집자 / Haewol 작가 / Cody 개발자 / 커스텀 문어)
- 🎯 이름·역할·성격(MBTI 16종)·외형·모델을 시각적으로 설정
- 💬 더블클릭/말풍선으로 대화 — 응답에 따라 머리 위 **감정 이모지**(12종)가 실시간 변화
- 📝 책상 위 메모지로 직원 관리 + 기본 감정 설정
- 🛍 상점에서 가구 구매 → 드래그/배치로 내 사무실 꾸미기
- 🌙 토큰 고갈 시 강제 야간 (시그니처 폴리시)

**차별점**: 단순 채팅 도구가 아닌 *"애착이 생기는 환경"*. 사용자가 자기 사무실을 자랑하게 됨.

**현재**: 설치 없이 더블클릭으로 실행되는 portable `.exe`(v0.0.1)를 외부 테스터에게 배포해 실사용 피드백을 받는 단계.

---

## 1. Problem · 풀고자 한 문제

### 1.1 외부 문제 (사용자 관찰)

- **AI 에이전트 도구의 진입 장벽**: 대부분의 멀티 에이전트 도구(AutoGen, CrewAI 등)가 개발자 대상. 일반 사용자는 코드 작성·YAML 설정·터미널 실행을 못 함.
- **하지만 일반 사용자도 "페르소나 대화"는 이미 잘 쓴다**: ChatGPT/Claude 앱에서 "당신은 편집자입니다", "당신은 철학자 칸트입니다" 같은 시스템 프롬프트 활용.
- **격차**: 페르소나 대화 사용자 ↔ 멀티 에이전트 도구 → 중간 다리가 없음.

### 1.2 관찰 사례
> 본 프로젝트 사용자의 동생: 글 작업할 때마다 ChatGPT 채팅창에 "당신은 편집자입니다. 원고를 받으면..." 를 반복 입력. **같은 페르소나를 재사용하고 싶지만 채팅창 분리가 안 됨.**

### 1.3 기존 솔루션 한계

| 기존 도구 | 한계 |
|---|---|
| Claude.ai Projects | 페르소나 분리는 OK, 다만 멀티 에이전트 X, 시각적 매력 X |
| AutoGen / CrewAI | 개발자 전용, 시각화 X |
| ChatGPT Custom GPTs | 분리 OK, 무료 사용자 제한 |
| Character.ai | 캐릭터 매력 ↑, 작업 도구로는 약함 |

→ **"애착 가질 수 있는 작업 도구"의 빈자리.**

---

## 2. Vision · 차별화

### 2.1 한 줄 비전

> "*Two Point Hospital + The Sims + AI Town*의 게임 메커니즘으로 AI 에이전트 관리를 풀어낸다."

### 2.2 핵심 차별점 3가지

| | 기존 도구 | PixelAgentOffice |
|---|---|---|
| **설정** | 코드/YAML 작성 | 캐릭터 선택 + 이름 짓기 + MBTI 부여 |
| **관리** | JSON 그래프 / 명령어 | 책상 위 📝 메모지 클릭 |
| **모니터링** | 로그 응시 | 사무실 둘러보기 + 직원 감정 이모지 |
| **에러** | 빨간 메시지 | 직원이 시무룩해함 / 친절 한글 안내 |

### 2.3 디자인 원칙

1. **사용성 + 재미를 동시에** — 기능만 동작하는 도구가 아닌, *내 사무실에 애착이 생기는* 환경.
2. **사용자 부담 최소화** — 사용자는 "지침"만 직접 관리, "메모리"는 시스템이 백그라운드 처리(계획).
3. **점진적 노출** — 첫 실행 시 빈 사무실 + 채용 안내. 익숙해지면 상점·MBTI·감정 등 게임적 요소 자연스럽게 확장.
4. **픽셀 일관성** — 모든 요소 (캐릭터·책상·가구·UI)를 같은 픽셀 그리드 시스템으로 통일. (단, 12×12 그리드의 시각 한계 → 고해상도 디테일은 PNG asset 도입 시 확장 예정)

---

## 3. Target Users · 페르소나

### 3.1 Primary — *"페르소나 채팅을 일상적으로 쓰는 일반 사용자"*

| 항목 | 내용 |
|---|---|
| 직군 | 작가, 학생, 디자이너, 마케터, 1인 사업자 |
| LLM 사용 경험 | ChatGPT/Claude 무료 또는 Pro 구독 사용 중 |
| 페인포인트 | 페르소나마다 채팅창 분리 안 됨, 매번 시스템 프롬프트 복붙, 캐릭터 일관성 부족 |
| 기대 | "내 글쓰기 도우미 Mary"가 매번 같은 톤으로 답해주길 |
| 기술 수준 | 코드 못 씀, GUI만 가능 |

### 3.2 Secondary — *"AI 도구 얼리어답터 / 개발자"*

| 항목 | 내용 |
|---|---|
| 직군 | 개발자, AI 엔지니어, 프로덕트 매니저 |
| LLM 사용 경험 | API 키 사용 익숙, 여러 모델 비교 |
| 페인포인트 | 자기 워크플로우에 맞는 에이전트 도구 부족, 멀티 에이전트 시각화 도구 부족 |
| 기대 | 자기 프로젝트 에이전트들을 시각화하고 싶음 (Phase 2: Seegene 같은 케이스) |
| 기술 수준 | 코드 작성 가능 |

### 3.3 Anti-Persona — 본 도구가 적합하지 않은 사람

- 단순히 한 번 쓰고 마는 사용자 (커스터마이징 가치 0)
- 진지한 협업 워크플로우 필요 (B2B SaaS 영역)

---

## 4. Key Features · 기능

> 범례: ✅ 완성·검증 / 🟡 부분 구현(골격·UI는 있으나 핵심 로직 미연결) / 📋 계획

### 4.1 현재 완성 (M0~M5 + Day 7~12, v0.0.1 기준)

#### 기반 (M1~M3)
| 영역 | 기능 | 상태 |
|---|---|---|
| 기본 동작 | Electron 데스크탑 앱, **빈 사무실로 시작**(테스터가 직접 첫 채용) | ✅ |
| 픽셀 사무실 | Phaser 4 기반 픽셀 그리드 사무실 (캐릭터·책상·창문 풍경·시계·구름) | ✅ |
| 채용 | 템플릿(Mary 편집자 / Haewol 작가 / Cody 개발자 / 커스텀) → 이름·역할·직급·진급방식·MBTI·외형·모델 | ✅ |
| 기본값 자동 채움 | 기본 캐릭터 선택 시 이름·역할·지침·색·무늬 자동 입력 | ✅ |
| 지침 편집 | 메모지 → 기본 지침(읽기 전용) + 커스텀 지침(편집) | ✅ |
| 다중 LLM | Anthropic Claude(Opus/Sonnet/Haiku) + Google Gemini(2.5 Pro/Flash) | ✅ |
| API 키 보안 | OS 키체인(safeStorage) provider별 암호화 저장 | ✅ |
| 영속화 | JSON 파일(`%APPDATA%`)에 직원·설정·채팅 이력·배치 가구 저장 | ✅ |

#### LLM 안정성·UX (M4)
| 영역 | 기능 | 상태 |
|---|---|---|
| Rate limit | sliding-window RPM 사전 차단, 친절 한글 에러(raw URL 노출 차단) | ✅ |
| 채팅 중단 | AbortController 체인 | ✅ |
| 사용량 시각화 | 칩/토글 + 비용 추정 + 설정 모달 상세 표(모델별 RPM 막대) | ✅ |
| 페르소나 자리비움 | 14종 랜덤 게임 상태 메시지(☕커피/🚽화장실 등) | ✅ |
| 채팅 영속화 | employee별 대화 이력 보존 (앱 재시작 후도 유지) | ✅ |

#### 사무실·캐릭터 (M5 + Day 7~11)
| 영역 | 기능 | 상태 |
|---|---|---|
| 자리/팀 시스템 | 사장 1 + 3팀 × 5명 = 16자리, 직급 자격(과장↑ 리더, 사장↑ 사장석), 팀 동적 중앙정렬 | ✅ |
| 자리 이동 | 우클릭 → 드래그앤드롭 자리 변경 + 자격 검증 | ✅ |
| 책상 회전 | front/right/left 순환 (캐릭터·우클릭 zone 함께 회전) | ✅ |
| 시간대 | 5단계 자동 전환(아침/점심/노을/저녁/밤) RGB 보간 | ✅ |
| 토큰 보드 | 사장석 뒤 LED 액자, 비용 임계 🟢🟡🔴 + 깜빡임 | ✅ |
| **강제 야간** | 토큰 고갈 시 사무실 어두워짐 (시그니처 폴리시) | ✅ |
| 줌·카메라 | 휠 줌 + 토글 버튼 (핀치는 모바일 단계로 보류) | ✅ |
| 팻말·이름 수정 | 팀 라벨 우클릭 → 이름 수정 모달 | ✅ |

#### 감정·성격 (Day 11~12) ★ v0.0.1 핵심
| 영역 | 기능 | 상태 |
|---|---|---|
| **감정 시스템** | 말풍선 12종 이모지(😄😴🤔💡❤️😡 등). LLM 응답에 `[emotion:xxx]` 태그 강제 → 파싱 → 5초 표시 → 기본 감정 복귀 | ✅ |
| 기본 감정(idle) | 직원별 평소 표정을 메모 모달에서 12종 중 선택 | ✅ |
| **MBTI 16종 페르소나** | 채용 시 MBTI 부여 → LLM system prompt에 페르소나 지침 자동 주입 | ✅ |

#### 상점·꾸미기 (Day 11~12)
| 영역 | 기능 | 상태 |
|---|---|---|
| 상점 | 가구 카탈로그 + 실제 사무실과 동일한 픽셀 미리보기 | ✅ |
| 가구 배치 | 클릭 위치에 배치(ghost preview), 드래그 이동, 우클릭 메뉴(옮기기/삭제/전체삭제) | ✅ |
| 감정 미리보기 | 12종 감정을 캔버스로 즉시 미리보는 모달 | ✅ |

#### 모달·온보딩 UX (Day 8~12)
| 영역 | 기능 | 상태 |
|---|---|---|
| 온보딩 | 빈 사무실일 때 "+ 채용" 펄스 + 안내 박스 | ✅ |
| 채용 검증 | 필수값(이름·역할) 빈칸 빨간 강조 + 중첩 알림 모달 | ✅ |
| 키 없는 모델 | 비활성 + ⚙ 설정 열기(채용 모달 입력 보존한 채 중첩) | ✅ |
| 모달 안정성 | backdrop 드래그 시 안 닫힘(mousedown 기준), 설정 모달 채용 위 중첩 | ✅ |

#### 배포 (Day 12) ★
| 영역 | 기능 | 상태 |
|---|---|---|
| **Platform Adapter** | Electron API를 환경별 adapter로 추상화(미래 모바일 진입 격리) | ✅ |
| **portable EXE** | `electron-builder` → `pnpm dist:exe` → 설치 없는 단일 `.exe`(v0.0.1) | ✅ |
| **GitHub Releases 배포** | v0.0.1 EXE 첨부 + 테스터 안내 → 외부 실사용 피드백 수집 중 | ✅ |
| 자동 테스트 | Playwright E2E(Electron 실행 기반) — 현재 빈 사무실 전환에 맞춰 재작성 진행 중 | 🟡 |

### 4.2 부분 구현 — 골격은 있으나 핵심 로직 미연결 🟡

> 포트폴리오 정직성을 위해 명시. 상세는 [HANDOFF "미완·멈춤 레지스터"] 및 본 폴더 `FEATURES.md` 참조.

| 기능 | 있는 것 | 빠진 것 |
|---|---|---|
| **직급 진급** | 9단계 직급 + 진급방식 5종 선택 UI + 자격 기반 자리 제한 | 진급 로직·`totalMessages` 카운트·진급 요청 모달·콘페티 |
| **메모리 시스템** | 4모드(off/manual/ask/auto) UI + 메모리 전용 모델 | 실제 기억 저장·요약·system prompt 주입 |
| **칭찬 카운트** | "받은 칭찬 N회" 표시 + 정성형 진급 모드 | 👍 버튼·키워드 감지 |
| v2.5 외형 디테일 | 액세서리·책상 소품·눈 표정 코드 보존 | 12×12 그리드 한계로 시각 비활성 (PNG asset 대기) |

### 4.3 계획 (M6~)

- **M6 진급 시스템 완성**: 활동 카운터 연결 → 진급 요청 모달 + 콘페티 (현재 부분구현 → 완성)
- **M7 메모리 시스템**: 4모드 실제 동작 + 야간 자동 압축
- **Phase 3 백엔드**: BYOK 프록시 서버 → 모바일 진입 기반
- **Phase 5 모바일**: Tauri/Capacitor → iOS/Android
- **PNG asset 도입**: 캐릭터/가구 고해상도 → v2.5 외형 부활

### 4.4 기능 우선순위 의사결정

기능을 추가할 때마다 3가지 질문:
1. **사용자에게 *진짜* 가치인가?** (재미만으론 부족, 작업 가치도 ↑)
2. **현재 단계에 필요한가?** (테스터 배포엔 빈 사무실→채용→대화 핵심 흐름 우선)
3. **다른 기능과 시너지인가?** (예: 감정 시스템 ↔ MBTI = 캐릭터 개성의 통합)

---

## 5. User Journeys · 사용자 여정

> v0.0.1 실제 흐름 기준. 상세 플로우 다이어그램은 [user-flow.md](user-flow.md) 참조.

### 🎬 Journey 1 — 신규 테스터 첫 30분 (현재 빌드)

```
1. GitHub Releases에서 PixelAgentOffice-0.0.1-portable.exe 다운로드 → 더블클릭
   (SmartScreen 경고 시 "추가 정보 → 실행" 안내 동행)
2. 빈 사무실 화면 — "+ 채용" 버튼이 노랗게 펄스 + 안내 박스
3. + 채용 클릭 → 채용 모달
   - 기본 캐릭터(Mary/Haewol/Cody) 선택 → 이름·역할·지침·외형 자동 채움
   - MBTI 입력(선택) + 대화 모델 선택
   - 키 없으면 모델 비활성 + ⚙ 설정 열기(입력 유지한 채 설정 중첩)
4. ⚙ 설정에서 API 키 등록 → 닫으면 채용 모달 입력 그대로 → 채용 완료
5. 사무실에 캐릭터 등장 → 더블클릭/말풍선 클릭 → 채팅창
6. "안녕!" → 응답 도착 → 머리 위 감정 이모지 5초 표시 → 기본 표정 복귀
7. 📝 메모지 → 커스텀 지침/기본 감정 변경 → 톤·표정 변화 확인
8. 🛍 상점 → 가구 구매 → 클릭 배치 → "내 사무실" 감각
```

### 🎬 Journey 2 — 글쓰기 작업 시나리오

```
1. Haewol(작가)의 💬 클릭 → 채팅
2. 원고 붙여넣기 → "이 글에 비유 한 개 추가해줘"
3. Haewol 응답 — 바다·별 비유 + 머리 위 💡(idea) 이모지
4. MBTI/지침에 따라 톤 일관 유지
5. (계획) 칭찬 누적 → 진급 요청 모달
```

### 🎬 Journey 3 — 야간 시그니처

```
1. 사용자가 채팅 누적 → 비용 임계 접근 → 토큰 보드 🟡→🔴 깜빡임
2. 한도 도달 → 강제 야간: 사무실 어두워짐
3. (계획) 각 캐릭터 성격대로 반응 + 야간 메모리 압축
```

### 🎬 Journey 4 — Seegene 멀티 에이전트 임포트 (Phase 2)

```
1. 엘리베이터 → Floor 2 (Team Office)
2. `.claude/agents/` 폴더 임포트 → 서브에이전트 자동 배치
3. Leader가 워커에게 분배 → 결과 검수 보고
```
*(Phase 2 계획 — 미구현)*

---

## 6. Information Architecture · 구조

### 6.1 화면 계층

```
App Shell
├─ TopBar (직원 카운터 / + 채용 / 🛍 상점 / ⚙ 설정)
├─ Stage (Phaser 사무실 씬: 캐릭터·책상·가구·시간대·토큰보드·감정 말풍선)
├─ ChatPopup (우측 하단 채팅창)
└─ StatusBar (직원수·시간대·누적 비용 라이브)

모달 레이어
├─ HireModal (채용) ─┬─ 알림 모달(중첩) · MBTI 설명 모달(중첩)
├─ SettingsModal (설정, 채용 위 중첩 가능) ─ 사용량 상세
├─ MemoModal (메모지: 지침·기본감정·모델·해고)
├─ ShopModal (상점) ─ EmotionPreviewModal (감정 미리보기)
└─ 컨텍스트 메뉴 (캐릭터 우클릭 / 가구 우클릭)
```

### 6.2 데이터 모델 (현재)

```typescript
type Employee = {
  id: string
  template: 'editor' | 'writer' | 'developer' | 'custom'
  name: string
  role: string
  emoji: string
  baseInstructions: string       // 읽기 전용 (정체성)
  customInstructions: string     // 사용자 편집
  model: Model                   // Claude/Gemini
  memoryModel: Model
  memoryMode: 'off' | 'manual' | 'ask' | 'auto'   // 🟡 UI만, 로직 계획
  mbti?: MBTI                    // 16종 페르소나
  idleEmotion?: BubbleEmotion    // 평소 말풍선 감정 (12종)
  customColor?: string           // 커스텀 외형
  pattern?: CharacterPattern     // solid/speckled/gradient/stripes
  seatId: SeatId                 // 'boss' | 'leader:A' | 'member:A:0' ...
  deskOrientation: 'front' | 'right' | 'left'
  rank: Rank                     // 알바~레전드 (🟡 진급 로직 계획)
  promotionMode: PromotionMode   // 🟡
  hiredAt: ISO
  totalMessages: number          // 🟡 카운트 미연결
  totalMemoryUpdates: number     // 🟡
  totalPraises: number           // 🟡
}

type Settings = {
  defaultModel: Model
  defaultMemoryModel: Model
  dailyLimitUsd: number
  usageDisplayMode: 'chips' | 'toggle'
  teamNames: Record<TeamId, string>
  placedFurniture: PlacedFurniture[]   // 사무실 배치 가구
  // API 키는 별도 OS 키체인
}
```

### 6.3 3-레이어 컨텍스트 + 페르소나 (LLM 시스템 프롬프트 구성)

```
[Layer 1] 기본 지침   — 채용 시 결정, 읽기 전용 (캐릭터 정체성)
[Layer 2] 커스텀 지침 — 사용자가 직접 편집 (행동 규칙)
[Layer 3] MBTI 페르소나 — mbti 있으면 성향·대답 방식 자동 주입
[Layer 4] 감정 태그 지침 — 응답 마지막에 [emotion:xxx] 강제
[Layer 5] 자동 메모리 — 🟡 계획 (M7)
       모두 합쳐서 → Claude/Gemini API에 system prompt로 전송
```

---

## 7. Technical Architecture

```
┌──────────────────────────────────────────────────┐
│         PixelAgentOffice-0.0.1-portable.exe        │
│                                                    │
│  ┌─ Renderer (Chromium) ─────────────────────┐     │
│  │  React 19 + Phaser 4                       │     │
│  │  ├─ Phaser canvas (사무실 씬)              │     │
│  │  └─ React DOM (모달/채팅/상단바)            │     │
│  │  └─ src/platform/ (Platform Adapter)       │     │
│  └────────────┬───────────────────────────────┘     │
│               │ IPC (preload.ts) / platform.* 추상화 │
│  ┌────────────▼─────────────────────────────────┐   │
│  │  Main Process (Node.js)                      │   │
│  │  ├─ data/store.ts (JSON 영속화)              │   │
│  │  └─ llm/ dispatch·anthropic·gemini·usage·    │   │
│  │     errorMessages·apiKeys(safeStorage)       │   │
│  └──────────┬───────────────────────────────────┘   │
└─────────────┼─────────────────────────────────────┘
              │ HTTPS
       ┌──────▼──────────┐
       │  Anthropic /    │
       │  Google         │
       └─────────────────┘
```

**핵심 디자인 결정**:
- **Hybrid 렌더링**: Phaser(게임 씬) + React(UI 모달). 각자 강점.
- **Platform Adapter**: `src/platform/`로 Electron API 격리 → 미래 Web/모바일 진입 시 컴포넌트 코드 무변경.
- **Provider Abstraction**: `LLMProvider` 인터페이스로 Groq/Ollama 추가 용이.
- **API 키 분리 저장**: provider별 별도, OS 키체인 암호화.
- **픽셀 렌더링**: `drawPixelGrid()` 문자열 배열 → 픽셀 사각형. 12×12 한계는 PNG asset로 확장 예정.
- **배포**: `electron-builder` portable target → 설치 없는 단일 `.exe`. 코드 사인/자동 업데이트는 후속.

### 7.1 Electron이란? (비개발자용 설명)

> **Electron** = 웹 기술(React/HTML/CSS)로 데스크탑 앱(.exe)을 만드는 프레임워크. VS Code, Discord, Slack, Notion이 모두 Electron 기반.

**왜 골랐나**: `.exe` 더블클릭으로 즉시 실행 + 웹 기술 재사용 + OS 키체인/파일 접근. **한계**: iOS/Android 미지원(데스크탑 전용) → 모바일은 Platform Adapter로 격리해 후속 대응.

### 7.2 모바일/태블릿 출시 전략

모바일은 Electron만으론 불가 → 빌드 도구 교체 + 백엔드 추가. 단 **설계 재작성 없음(~85% 재사용)**. Platform Adapter(Day 8 도입 완료)로 환경 의존을 이미 격리. 점진 단계: Web 빌드 → 백엔드 프록시 → 인증/키 저장 → Tauri/Capacitor 빌드.

→ 자세히 [`../v0.0.0/planning/13-electron-and-mobile-strategy.md`](../v0.0.0/planning/13-electron-and-mobile-strategy.md)

---

## 8. Business Model

### 8.1 검증된 한계
> "이미 Claude를 쓰는 사람에게 UI만 무료 제공" → **Anthropic ToS상 불가능**.

### 8.2 BYOK (Bring Your Own Key) 모델 채택
- 사용자가 직접 API 키 발급 + 결제 / 우리는 비용·법적 책임 0
- 단점: 카드 등록 부담 → 백엔드 프록시(Phase 3)로 완화 검토

### 8.3 진입 장벽 완화 — 옵션 분석

| 옵션 | 카드 등록 | 작업 | 경험 |
|---|---|---|---|
| A. BYOK 유지 (현재) | 필수 | 0 | 개발자/얼리어답터 |
| B. 백엔드 SaaS | 0 | 며칠 | 진짜 무료, 사업 결심 |
| C. Groq 추가 | 불필요 | 5분 | 무료, 품질 ↓ |
| D. 데모 모드 | 불필요 | 30분 | 둘러보기 |

→ 모바일 진입과 함께 **B(백엔드 + BYOK 프록시)** 로 결정. 현재는 A(BYOK)로 테스터 배포.

---

## 9. Success Metrics · 성공 지표

### 9.1 학습/포트폴리오 단계 (현재 달성)

| 지표 | 측정 |
|---|---|
| ✅ 풀스택 데스크탑 앱 | Electron/React/Phaser/LLM + Platform Adapter |
| ✅ 다중 LLM | Claude + Gemini |
| ✅ 시그니처 폴리시 | 시간대/토큰보드/강제야간/감정/MBTI |
| ✅ **실배포** | portable EXE v0.0.1 GitHub Releases → 외부 테스터 |
| ✅ 의사결정 문서화 | 19개 기획 문서 + 세션 로그(Day 1~12) |

### 9.2 배포 단계 가설 (미래)

| 지표 | 가설값 |
|---|---|
| 첫 채팅까지 시간 | < 5분 (다운로드 → 첫 응답) |
| 테스터 완주율(채용→대화) | 추적 시작 (v0.0.1 피드백) |
| 채용 직원 평균 | 1.5명 / 사용자 |
| 7일 리텐션 | 20% 이상 |

---

## 10. Roadmap · 마일스톤

```
✅ M0 기획            ─ 19개 디자인 문서 + 와이어프레임 + mockup
✅ M1 기본 UI         ─ Electron + Phaser + React, mock 응답
✅ M2 UI 채널         ─ 채용/메모/설정 모달, JSON 영속화
✅ M3 다중 LLM        ─ Claude + Gemini, OS 키체인, E2E
✅ M4 Rate-limit UX   ─ RPM 차단, 친절 에러, 사용량 시각화
✅ M5 시그니처        ─ 자리/팀, 시간대, 토큰보드, 강제야간
✅ Day 7~8           ─ 자리이동·책상회전, Platform Adapter
✅ Day 10~11         ─ layout, 팻말, v2.5(보존), 가구 배치
✅ Day 12 (v0.0.1)   ─ 감정 12종 + MBTI 16종 + 상점 + 모달 UX + EXE 배포
🟡 M6 진급           ─ 활동 카운터 연결 + 진급 요청 모달 (부분→완성)
📋 M7 메모리          ─ 4모드 실동작 + 야간 압축
📋 Phase 3 백엔드     ─ BYOK 프록시 (모바일 기반)
📋 Phase 5 모바일     ─ Tauri/Capacitor
📋 PNG asset          ─ 고해상도 캐릭터/가구 → v2.5 부활
```

---

## 11. Risks · 위험 및 미정 사항

### 11.1 기술적 위험

| 위험 | 영향 | 완화책 |
|---|---|---|
| 픽셀 12×12 시각 한계 | 중 | PNG asset 도입 시 부활 (코드 보존 완료) |
| EXE 코드 사인 부재 | 중 | SmartScreen 경고 → 안내문 동행, 추후 인증서 |
| LLM API 정책 변화 | 중 | Provider abstraction |
| 미완 기능(진급/메모리) 노출 | 저 | UI는 있으나 로직 미연결 — 레지스터로 추적·정직 표기 |

### 11.2 비즈니스 위험

| 위험 | 영향 | 완화책 |
|---|---|---|
| 사용자 진입 부담(결제) | 큼 | 백엔드 프록시(Phase 3) / 데모 모드 |
| 기능 과다 → 첫 사용자 압도 | 중 | **테스터 피드백 T1: 튜토리얼/온보딩 필요** (대응 예정) |
| Clawd 캐릭터 라이선스 | 저 | 배포 직전 자체 캐릭터 교체(swappable) |

### 11.3 미결정 사항

- [ ] 튜토리얼/온보딩 강화 방식 (테스터 T1 피드백)
- [ ] MBTI 별명 톤 (게임형/한국밈/직업)
- [ ] 백엔드 SaaS 전환 시점
- [ ] Groq/데모 모드 추가
- [ ] Phase 2 Seegene 임포트 인증

---

## 12. Appendix · 부록

### 12.1 의사결정 일지 (요약)

전체 흐름은 세션 로그(brainstorming-log, Day 1~12) 참조. 핵심 전환점:
- 🔄 *"제약이 결정을 명확하게 했다"* — 배포 자동화 요구 → Electron 채택
- 🔄 *"사용자 인사이트가 아키텍처를 단순화"* — 동생 페르소나 채팅 → 이중 모드
- 🔄 *"에러가 폴리시로 격상"* — 토큰 고갈 → 강제 야간 시그니처
- 🔄 *"감정/MBTI가 캐릭터 개성으로"* — 12 emotion 픽셀 → 자동 트리거 + MBTI 페르소나로 살림 (Day 12)
- 🔄 *"실배포로 검증 사이클 진입"* — portable EXE v0.0.1 → 외부 테스터 피드백

### 12.2 시각 자료 (v0.0.1)

| 문서 | 설명 |
|---|---|
| 📐 [wireframes.html](wireframes.html) | 현재 화면 와이어프레임 |
| 🔀 [user-flow.md](user-flow.md) | 사용자 플로우 다이어그램 |
| 📋 [FEATURES.md](FEATURES.md) | 기능별 검증 체크리스트(최신) |
| 📦 [code-snapshot/](code-snapshot/) | v0.0.1 코드 스냅샷 |

### 12.3 코드 산출물

| 위치 | 내용 |
|---|---|
| [code-snapshot/](code-snapshot/) | v0.0.1 전체 코드 (src + electron + tests) |
| [../v0.0.0/milestones/](../v0.0.0/milestones/) | M1~M5 마일스톤별 스냅샷 + 회고 (초기) |

### 12.4 정직성 노트 (포트폴리오용)

본 PRD는 "완성된 것처럼 보이기"보다 **현재 상태를 정확히** 기록한다:
- ✅ 완성·검증 / 🟡 부분 구현(UI만) / 📋 계획 을 명확히 구분
- 미완 기능(직급 진급·메모리·칭찬 카운트)은 "골격은 있으나 활동 추적 미연결"로 솔직히 표기
- 이는 *"무엇을 만들었나"* 만큼 *"무엇이 남았는지 정확히 아는가"* 를 보여주는 기획 역량의 일부

---

## 🎯 핵심 메시지 (기획자 관점에서 자랑스러운 것)

1. **사용자 행동 관찰에서 시작** — 동생의 페르소나 채팅 패턴 → 시장 빈자리 발견
2. **제약을 일찍 파악** — 배포 자동화 / Anthropic ToS / Gemini quota 사전 검증
3. **재미를 핵심 가치로 격상** — 토큰 고갈을 시그니처로, 감정·MBTI로 캐릭터 개성 부여
4. **점진적 단계화** — M0 기획 → M5 시그니처 → **실제 EXE 배포**까지, 매 단계 작동 결과물 + 회고
5. **미래 대비 설계** — Platform Adapter로 모바일 진입 비용 사전 격리
6. **실배포로 검증 사이클 진입** — v0.0.1을 외부 테스터에게 배포해 실사용 피드백 수집
7. **정직한 상태 추적** — 완성/부분/계획을 구분하고 미완 기능을 레지스터로 관리

---

**작성**: 2026-05-26 (Day 12, v0.0.1)
**이전 버전**: [v0.0.0 PRD](../v0.0.0/PRD.md) (2026-05-18, M3 시점)
**다음 갱신**: M6 진급 완성 또는 튜토리얼 도입 시점
